"""Phase 6 — Multi-Brand / Multi-Channel / Portfolio / Monetization engines.

Deterministic where possible (SQL + metrics + rules + scoring). LLM use is
reserved for complex creative/reposition strategy only. Tenant isolation is
enforced at every scoped resource via `app.mb.scope`.
"""
